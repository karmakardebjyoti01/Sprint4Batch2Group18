import static io.restassured.module.jsv.JsonSchemaValidator.matchesJsonSchemaInClasspath;

given().spec(ApiConfig.REQUEST).auth().oauth2(token)
.when().get("/viewPassengerById/9")
.then()
  .statusCode(200)
  .body(matchesJsonSchemaInClasspath("schemas/passenger-schema.json"));
